const connect = async() => {
    console.log("Mocked Connection")
    // will have to have the disconnect and connect as well with findUser and saveUser

    // findUser is going to return a promise with a result
    // expect firstName lastName
}